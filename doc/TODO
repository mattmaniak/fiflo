fiflo - TODOs and ideas:
- colorized status,
- tab with the gap,
- simplify config.c and buffer.h,
- all escapes and formatting control chars,
- pre-allocation of a memory, eg. next 16 lines,
- cut/copy/paste,
- improve vertical scroll - hard or soft with prev/next line preview,
- fully adaptive cursor during the vertical scroll,
- load only part of a file (nearly-infinite buffer),
- syntax highlighting,
- search option,
- hex mode,
- cursor at the beginning by default,
- refresh the git branch on the separate thread.
- CRLF support,
- moving the cursor without the window redrawing,
- cursor scroll in the live filename edit mode,
- real wrap guide,
- distinguish indentation type,
- reimplement Ctrl^Arrow up/down.
